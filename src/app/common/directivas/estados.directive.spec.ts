import { EstadosDirective } from './estados.directive';

describe('EstadosDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadosDirective();
    expect(directive).toBeTruthy();
  });
});
